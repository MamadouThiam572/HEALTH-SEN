<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.chart.LineChart?>
<?import javafx.scene.chart.NumberAxis?>
<?import javafx.scene.chart.CategoryAxis?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.*?>
<?import javafx.scene.shape.Circle?>
<?import javafx.scene.text.Text?>

<BorderPane fx:id="mainContainer" xmlns="http://javafx.com/javafx/17" 
            xmlns:fx="http://javafx.com/fxml/1" 
            fx:controller="com.senegalsante.controller.javafx.DashboardController"
            styleClass="main-container">
    
    <!-- MENU LATÉRAL MODERNE (SIDEBAR) -->
    <left>
        <VBox styleClass="sidebar" spacing="10">
            <!-- Logo area -->
            <VBox alignment="CENTER" spacing="10">
                <padding><Insets bottom="40"/></padding>
                <ImageView fitWidth="100" preserveRatio="true">
                    <image>
                        <Image url="@../images/logo.png"/>
                    </image>
                </ImageView>
                <Label text="HEALTHSen" style="-fx-text-fill: white; -fx-font-weight: 800; -fx-font-size: 18px; -fx-letter-spacing: 1px;"/>
                <Label text="La Santé Est Prioritaire" style="-fx-text-fill: #94a3b8; -fx-font-size: 11px;"/>
            </VBox>
            
            <Button text="🏠  Tableau de Bord" styleClass="sidebar-item, sidebar-item-active" maxWidth="Infinity" alignment="CENTER_LEFT"/>
            <Button fx:id="medicationsButton" text="💊  Médicaments" styleClass="sidebar-item" maxWidth="Infinity" alignment="CENTER_LEFT" onAction="#handleMedications"/>
            <Button fx:id="appointmentsButton" text="📅  Rendez-vous" styleClass="sidebar-item" maxWidth="Infinity" alignment="CENTER_LEFT" onAction="#handleAppointments"/>
            <Button fx:id="healthTrackingButton" text="❤️  Suivi Santé" styleClass="sidebar-item" maxWidth="Infinity" alignment="CENTER_LEFT" onAction="#handleHealthTracking"/>
            <Button fx:id="familyButton" text="👨‍👩‍👧  Ma Famille" styleClass="sidebar-item" maxWidth="Infinity" alignment="CENTER_LEFT" onAction="#handleFamily"/>
            <Button fx:id="prescriptionsButton" text="📋  Ordonnances" styleClass="sidebar-item" maxWidth="Infinity" alignment="CENTER_LEFT" onAction="#handlePrescriptions"/>
            
            <Region VBox.vgrow="ALWAYS"/>
            <Separator style="-fx-opacity: 0.1;"/>
            
            <Button fx:id="profileButton" text="👤  Paramètres" styleClass="sidebar-item" maxWidth="Infinity" alignment="CENTER_LEFT" onAction="#handleProfile"/>
            <Button fx:id="logoutButton" text="🚪  Déconnexion" styleClass="sidebar-item" maxWidth="Infinity" alignment="CENTER_LEFT" onAction="#handleLogout" style="-fx-text-fill: #ef4444;"/>
        </VBox>
    </left>
    
    <!-- CONTENU PRINCIPAL -->
    <center>
        <ScrollPane fitToWidth="true" style="-fx-background-color: transparent;">
            <VBox styleClass="content-area" spacing="30">
                
                <!-- ENTETE DE BIENVENUE PREMIUM -->
                <HBox styleClass="welcome-header" alignment="CENTER_LEFT" spacing="25">
                    <padding><Insets top="20" right="30" bottom="20" left="30"/></padding>
                    
                    <!-- Avatar Stylisé -->
                    <StackPane>
                        <Circle radius="35" style="-fx-fill: linear-gradient(to bottom right, #38bdf8, #0284c7);"/>
                        <Label text="👤" style="-fx-font-size: 30px; -fx-text-fill: white;"/>
                    </StackPane>
                    
                    <VBox spacing="2" HBox.hgrow="ALWAYS">
                        <Label fx:id="welcomeLabel" text="Bonjour, Daouda Ngom !" styleClass="label-title-modern"/>
                        <HBox alignment="CENTER_LEFT" spacing="8">
                            <Label text="✨" style="-fx-font-size: 14px;"/>
                            <Label text="Voici le résumé de votre santé pour aujourd'hui." styleClass="label-subtitle-modern"/>
                        </HBox>
                    </VBox>
                    
                    <!-- Date et Heure Discrètes -->
                    <VBox alignment="CENTER_RIGHT" spacing="5">
                        <Label fx:id="currentDateLabel" text="Jeudi 29 Janvier 2026" style="-fx-text-fill: -text-medium; -fx-font-weight: 600;"/>
                        <HBox alignment="CENTER_RIGHT" spacing="10">
                            <Label text="Lieu: Dakar, Sénégal" style="-fx-text-fill: -text-light; -fx-font-size: 11px;"/>
                        </HBox>
                    </VBox>
                </HBox>

                <!-- CARTES DE RÉSUMÉ (TOP) -->
                <HBox spacing="25">
                    <!-- Prochaine Prise -->
                    <VBox styleClass="summary-card, summary-blue" spacing="8" HBox.hgrow="ALWAYS" 
                          onMouseClicked="#handleMedications" style="-fx-cursor: hand;">
                        <Label text="PROCHAINE PRISE" styleClass="label-summary"/>
                        <Label fx:id="nextMedNameLabel" text="Aucune" styleClass="value-summary"/>
                        <Label fx:id="nextMedPatientLabel" text="" styleClass="label-summary-patient"/>
                        <Label fx:id="nextMedTimeLabel" text="Pas de prise prévue" styleClass="label-summary-footer"/>
                    </VBox>
                    
                    <!-- Prochain RDV -->
                    <VBox styleClass="summary-card, summary-green" spacing="8" HBox.hgrow="ALWAYS" 
                          onMouseClicked="#handleAppointments" style="-fx-cursor: hand;">
                        <Label text="PROCHAIN RENDEZ-VOUS" styleClass="label-summary"/>
                        <Label fx:id="nextAppointmentLabel" text="Dr. Sow" styleClass="value-summary"/>
                        <Label fx:id="nextAppointmentPatientLabel" text="" styleClass="label-summary-patient"/>
                        <Label fx:id="nextAppointmentDateLabel" text="Planifiez votre RDV" styleClass="label-summary-footer"/>
                    </VBox>
                    
                    <!-- État de Santé -->
                    <VBox fx:id="wellnessCard" styleClass="summary-card, summary-orange" spacing="10" HBox.hgrow="ALWAYS">
                        <Label text=" BIEN-ÊTRE GÉNÉRAL" styleClass="label"/>
                        <Label fx:id="healthScoreLabel" text="85 %" styleClass="value"/>
                        <Label fx:id="healthStatusLabel" text="Très bon état global" styleClass="label" style="-fx-font-size: 13px;"/>
                    </VBox>
                </HBox>

                <!-- SECTION GRAPHIQUE ET MÉDICAMENTS DU JOUR -->
                <HBox spacing="30">
                    <!-- Graphique (Gauche) -->
                    <VBox styleClass="card" spacing="20" HBox.hgrow="ALWAYS">
                        <Label text="Évolution de la Santé" styleClass="section-title"/>
                        <LineChart fx:id="healthChart" legendVisible="false" minHeight="300">
                            <xAxis><CategoryAxis side="BOTTOM"/></xAxis>
                            <yAxis><NumberAxis side="LEFT" lowerBound="0" upperBound="100"/></yAxis>
                        </LineChart>
                    </VBox>
                    
                    <!-- Médicaments du Jour (Droite) -->
                    <VBox styleClass="card" spacing="20" prefWidth="350">
                        <Label text="Médicaments du Jour" styleClass="section-title"/>
                        <VBox fx:id="dailyMedicationsContainer" spacing="15">
                            <!-- Contenu dynamique chargé par le contrôleur -->
                        </VBox>
                        <Button text="Voir tout le traitement" styleClass="btn-outline" maxWidth="Infinity" onAction="#handleMedications"/>
                    </VBox>
                </HBox>

                <!-- DERNIERS ENREGISTREMENTS -->
                <VBox styleClass="card" spacing="20">
                    <HBox alignment="CENTER_LEFT">
                        <Label text="Historique Récent" styleClass="section-title"/>
                        <Region HBox.hgrow="ALWAYS"/>
                        <Hyperlink text="Voir l'historique complet" style="-fx-text-fill: -secondary;"/>
                    </HBox>
                    <VBox fx:id="recentRecordsContainer" spacing="12">
                        <!-- Les cartes d'historique seront injectées ici -->
                    </VBox>
                </VBox>

            </VBox>
        </ScrollPane>
    </center>
    
</BorderPane>
